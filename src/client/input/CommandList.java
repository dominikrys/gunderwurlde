package client.input;

/**
 * CommandList enum. Contains different commands for sending requests to server
 */
public enum CommandList {
	ATTACK,
	CHANGEITEM,
	DROPITEM,
	MOVEMENT,
	RELOAD,
	TURN,
	CONSUMABLE;
}
