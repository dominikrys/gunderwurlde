package client.input;

/**
 * KeyAction enum. Contains all allowed actions that can be bound to a button.
 * @author Dominik Rys
 */
public enum KeyAction {
    UP, DOWN, LEFT, RIGHT, RELOAD, DROP, INTERACT, ITEM1, ITEM2, ITEM3, ESC
}
