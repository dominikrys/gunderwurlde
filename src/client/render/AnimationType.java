package client.render;

/**
 * AnimationType enum. Contains different types of animations that entities can have
 *
 * @author Dominik Rys
 */
public enum AnimationType {
    STAND, ATTACK, MOVE, RELOAD, DEATH, NONE
}
